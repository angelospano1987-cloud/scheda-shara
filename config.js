/* App key di Dropbox (pubblica per natura nel flusso PKCE: non e' un segreto). Vuota = sincronizzazione spenta. */
window.SHARA_DROPBOX_KEY = "9nq2k8g18ijhgaw";
/* Servizio di notifica di fine recupero (push-worker/). Vuoti = notifica spenta. La chiave VAPID pubblica non e' un segreto. */
window.SHARA_PUSH_URL = "";
window.SHARA_VAPID_KEY = "";
